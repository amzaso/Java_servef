Como funcionar con Git por comandos

Antes: debemos tener creado un repositorio nuevo y copiar lu enlace .git

1. vamos a la carpeta donde esta el contenido que queremo subir.
2. git init
3. git add * (incluimos todo)
4. git status (vemos todo lo que hemos añadido para sincronizar)
5. git commit -m 'comentario'
6. git remote add origin https://github...... (añadimos la ruta origen/remota donde volcar)
7. git pull origin master (primero hacemos un pull pq siempre hay algo en origin creado 2 txt)
8. git push origin master ( ya lo tenemos sincronizado)